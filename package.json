{
  "private": true,
  "scripts": {
    "lint": "eslint ./",
    "lint:report": "eslint --format gitlab ./",
    "install": "sudo npm install -g browser-sync",
	  "serve": "browser-sync start --server --files *.html *.js *.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.15.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-formatter-gitlab": "^2.1.0",
    "eslint-plugin-import": "^2.22.1"
  },
  "dependencies": {
    "bootstrap": "^5.1.3"
  }
}
